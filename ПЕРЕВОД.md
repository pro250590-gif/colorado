# ПЕРЕВОД САЙТА: КАК МЫ ЭТО ДЕЛАЕМ

Этот файл написан для того, кто пришёл на эту работу впервые — новый чат, новый
помощник, всё равно. Её условие 06.08.2026: **«я не могу каждый раз тебе заново
это всё объяснять; у тебя должна быть понятная логика, чтобы в следующий раз или
вообще другой искусственный интеллект сразу понял, где что мы делаем»**.

Значит читаешь этот файл ЦЕЛИКОМ до первой правки. Он короткий.

> ⭐ **ПРОДОЛЖАЕМ РАБОТУ? СРАЗУ В РАЗДЕЛ 8а — «ПОРЯДОК РАБОТ».** Там утверждённый
> клиентом план из девяти шагов с проверками: что уже закрыто, что следующее.
> План согласован целиком 07.08.2026 — заново его не обсуждаем, берём следующий
> невыполненный шаг. Разделы 1–8 объясняют, КАК устроен перевод; раздел 8а
> говорит, ЧТО делать сейчас.

---

## 1. НАЧНИ ОТСЮДА — ОДНА КОМАНДА

```
node i18n-table.js
```

Она открывает сайт дважды — по-русски и на языке — и собирает **`perevod.html`**:
таблицу «что переведено, а что нет». Это **единственная честная мера**. Не код,
не память, не ощущение.

⚠️ **Никогда не отвечай «переведено» по памяти или по коду.** Так уже ошиблись
дважды за один день: сначала «осталось просто пополнять словарь» (неверно),
потом отчитались о готовности, когда половина экрана была русской.

В таблице четыре состояния, и это важно — «не переведено» ≠ работа:

| статус | что значит | что делать |
|---|---|---|
| **переведено** | всё хорошо | ничего |
| **НЕ ПЕРЕВЕДЕНО** | наша недоделка | чинить, см. раздел 3 |
| **рамка наша, текст маршрута** | предложение переведено, внутрь подставлен русский текст автора | чинится переписыванием МАРШРУТА, не словарём |
| **содержимое маршрута** | описания дней и мест | писать заново по-английски, это отдельная работа (правило 21б) |

---

## 1а. ПОЧЕМУ ПРОВЕРКИ ПО ЭКРАНУ НЕ ХВАТАЕТ — И ЧТО ВМЕСТО НЕЁ

Её слова 07.08.2026: «опять часть текста не переведена… у тебя не получается
вытащить весь текст и перевести. Ищи и решай проблему».

**Причина была в самой мере.** Я мерила перевод ПРОГОНОМ СТРАНИЦЫ. Но страница
никогда не показывает всё сразу: окна, ошибки, подсказки в полях, ветки «рейс
вписан / не вписан», «машина есть / нет», гостевой режим, пустая поездка. Каждый
раз находилось новое — не потому, что я плохо искала, а потому что мера была
неполной ПО СВОЕЙ ПРИРОДЕ.

**Теперь мер две, и главная — по коду:**

```
node i18n-check.js      ← ПОЛНОТА. Должна показывать «БЕЗ ПЕРЕВОДА: 0»
node i18n-table.js      ← КАРТИНА для человека, perevod.html
```

`i18n-check.js` смотрит не на экран, а на index.html. Любой текст, который
человек когда-нибудь увидит, лежит в файле строкой — значит полный список даёт
файл. Для каждой русской строки проверка отвечает: есть ли у неё вообще путь к
переводу. Путей три: целиком в `DICT` · в `cx()` и в `CODE` · склеена с
разметкой, но все куски в `DICT`. Нет ни одного — ошибка со строкой кода.

⚠️ **Правило: `node i18n-check.js` должен возвращать ноль. Это условие выкладки,
а не пожелание.** Появилась новая русская строка в движке — проверка её поймает
до того, как это увидит клиент.

### 1б. ГЛАВНОЕ ПРАВИЛО ПРОВЕРКИ: ПЕРЕВОДИМ ВСЁ, ИСКЛЮЧЕНИЯ — ПОИМЁННО

Три раза подряд клиент находила русский текст там, где проверка молчала. Каждый
раз причина была ОДНА И ТА ЖЕ: я заранее решала, что «вот это переводить не
надо», и проверка честно туда не смотрела.

* «поле `nm` — это название места» → а в каталоге главной в том же поле лежало
  «Колорадо, США», «Юта, каньоны». Клиент нашла их на главной странице;
* «поле `q` — это поисковая строка» → а в анкете в том же поле лежали ВОПРОСЫ
  человеку: «куда едете?», «водите ли вы?»;
* «фраза есть в словаре — значит переведётся» → а она склеена с числом, и
  словарь до неё не достанет никогда.

⚠️ **Правило: по умолчанию переводится ВСЁ. Исключение — только там, где строка
точно оригинальное название (правило 10), и каждое исключение проверка печатает
ПОИМЁННО.** Сейчас их два: «км» и «ми». Спрятать что-то за общим числом больше
нельзя — если список исключений растёт, это видно сразу.

---

## 2. КАК УСТРОЕН ПЕРЕВОД — ТРИ СЛОЯ, А НЕ ОДИН

Это главное, что надо понять. Ошибка новичка — думать, что перевод = словарь.
**Словарём делается меньше половины.**

### Слой ① Словарь — `i18n.js`

Ключ — **сама русская фраза**, не выдуманный код. `'Где поесть': 'Where to eat'`.

Работает так: страница собирается как обычно, потом `applyDomT()` проходит по
готовому тексту и заменяет фразы по словарю. **Нет перевода — остаётся русский**,
поэтому сайт не может сломаться от недописанного словаря.

Словарь берёт только **целую фразу целым куском текста**. Он НЕ возьмёт
«45 мин» — потому что таких пар бесконечность.

⚠️ Повторяющийся ключ JS съедает молча: останется последний. Проверка встроена
в `node i18n-scan.js --left`.

⚠️ **С 08.08.2026 словарь языка лежит в своём файле** — `lang-en.js`, а для
нового языка `lang-<код>.js`. В `i18n.js` остался только механизм. Русский
посетитель чужих словарей больше не качает вовсе (правило 22г).

### Слой ② Словарь для кода — `CODE` в файле языка + `cx()` в `index.html`

Всё, что склеено с числом или именем: «45 мин», «Day 3», «city to airport: ».
Целым куском текста такое не бывает никогда, поэтому `DICT` его не берёт.

В коде стоит **`cx('русская строка')`**, а перевод лежит в разделе `CODE` файла
языка. ⚠️ **Добавить язык = положить файл. Код не трогается.**

У `cx()` есть второй довод — значения для меток: `cx('База {i} из {n}',{i,n})`.
Так фраза остаётся ЦЕЛОЙ, и слова внутри переставляются как надо языку. Склейка
плюсом порядок слов менять не даёт, поэтому рядом с ней всегда заводилась
развилка с готовым английским текстом.

Раньше здесь стояло 47 развилок `LANG !== 'ru' ? английский : русский` — и
третий язык получил бы английский просто потому, что он «не русский». Правило
22б: **если для нового языка надо править `index.html`, значит мы сделали
неправильно.**

⚠️ **С 08.08.2026 таких развилок в движке НЕ ОСТАЛОСЬ** (шаг 2 плана). Было 31,
из них 16 с английским текстом прямо в коде — они стали шаблонами. Развилка
`LANG === 'ru'` (у русского три формы слова, готовая строка из `open-hours.js`) —
безопасна по устройству: остальные языки идут общим путём. Развилка
`LANG !== 'ru'` — опасна, и теперь **каждая названа поимённо с причиной в
`i18n-check.js`**; новая роняет проверку в ошибку. Сейчас их две, и текста они
не выбирают. Порядок слов в дате и знак дробной части перестали быть развилками
вовсе — это запись языка (`LOCALE`).

Что уже сделано этим слоем (искать по именам функций):

* **время** — `minTxt` `durTxt` `fmtHM` `fmtH` `walkTxt` `hrsTxt`
* **даты** — `fmtDate` `fmtLong` `fmtShort` `dm` `dmRange` `dmy` `monthNm` `span2`
* **расстояния** — `fmtKm` `mrs` `convDist`
* **порядковые подписи** — `dayLbl` `daysLbl` `baseLbl` `ofTxt`
* **как едем** — `hopWord` `transWord` `roadBody` `guessBody`
* **часы работы** — `hoursChip` `offChip`
* **имена городов** — `baseName` (латиница из поля `q`), `homeNm` (из поля `en`)
* **множественное число** — `plural` + таблица `PLURAL_EN` в `i18n.js`

⚠️ Часы работы переводятся В `index.html`, а НЕ в `open-hours.js`. Тот файл общий
с проверками в терминале, у него самотесты на русских ответах. Разбор один на
всех — подпись своя.

### Слой ③ Три системные подпорки

Каждая закрывает целый класс, не трогай их без нужды:

1. **`trLine()`** — строка часто склеена через « · ». Целиком её в словаре быть
   не может, поэтому разбираем по разделителю и переводим куски.
2. **проход по атрибутам** внутри `applyDomT` — `placeholder`, `title`,
   `aria-label`. Подсказка в пустом поле тоже текст, но обход текста её не видит.
3. **`watchDomT()`** — наблюдатель за страницей. **Полстраницы рисуется ПОЗЖЕ**
   (слои карты, «Свернуть», списки в окнах), и первый проход её не видел: фразы
   были в словаре, а на экране оставались русскими.

---

## 3. ОНА ГОВОРИТ: «ВОТ ТУТ РУССКОЕ СЛОВО» — ЧТО ДЕЛАТЬ

Пошагово. Не угадывать.

1. **`node i18n-table.js`** — найди эту строку в `perevod.html`. Посмотри статус.
2. Если статус **«содержимое маршрута»** — скажи ей прямо: это не перевод, это
   переписывание маршрута (правило 21б). Не молчи и не чини втихую.
3. Если **«НЕ ПЕРЕВЕДЕНО»** — реши, какой это слой:
   * фраза целая, без чисел внутри → **словарь** `i18n.js`;
   * склеена с числом, датой, именем города → **`cx()` + раздел `CODE`** (слой ②).
4. Пиши перевод по **правилу 21** (см. ниже) — не дословно.
5. `node i18n-scan.js --left` — проверит словарь на повторяющиеся ключи.
6. `node build-langs.js` — пересобрать `/en/`. **Без этого правка не доедет.**
   Он печатает «разметка: переведено на сборке N, осталось русским M». **M должно
   быть 0.** Осталось — значит фраза доедет переводом уже в браузере, то есть
   с вспышкой русского.
7. Проверки: `node check-page.js`, прогон языка, прогон русской страницы.
8. `bash deploy.sh` — выложить. Потом **проверить САМ САЙТ**, а не «команда
   прошла».
9. `node i18n-table.js` ещё раз — убедиться, что число выросло.

---

## 3а. СОДЕРЖИМОЕ МАРШРУТА — ОТДЕЛЬНЫЙ ФАЙЛ РЯДОМ С МАРШРУТОМ

Описания дней и мест написал автор поездки, и они лежат в `trip-*.js`
по-русски. Английский вариант лежит рядом: **`trip-<маршрут>-<язык>.js`**.

Как это работает:

* файл — тот же словарь «русская фраза → перевод», формат `window.__tripT = {…}`;
* грузится **только на неродном языке** (`loadTripT` в `index.html`). Русский
  посетитель его не качает вовсе: Колорадо весит 105 КБ, платить трафиком за
  текст, который никогда не увидишь, — глупость (правило 22г);
* нет файла — молча остаёмся с русским содержимым, страница не ломается;
* ⚠️ **новый файл надо `git add`**, иначе `deploy.sh` его не увидит: список
  выкладки собирается через `git ls-files`, а он показывает только то, что в
  индексе. Проверено на себе: файл лежал на диске, сайт отдавал 404.

⚠️ **КЛЮЧОМ БЕРЁМ ТЕКСТ ТАК, КАК ОН СТОИТ НА СТРАНИЦЕ, А НЕ В ФАЙЛЕ ДАННЫХ.**
Заметка дня написана как `'<b>Выезжай рано.</b> К полудню очередь…'` — на экране
это ДВА куска текста, и ключ из файла данных до них не достаёт. Первый заход дал
30 совпадений из 95 ровно по этой причине.
Готовый список ключей выдаёт сама таблица:

```
node i18n-table.js trip-париж.js --content
```

**Сделано:** `trip-paris-en.js` — Париж, 95 из 95.

### 3б. ⚠️ ПОЛНОТА ТЕКСТА МАРШРУТА — ТОЖЕ ПО КОДУ

```
node i18n-check-trip.js      ← должно быть «не покрыто: 0»
```

Её слова 07.08.2026: «я посмотрела Колорадо и нашла там русский текст». И это
после моего отчёта «353 из 353». **Причина была та же, что у интерфейса: мера
по экрану.** Прогон открывает ленту, а часть текста маршрута живёт в вариантах
дня, подписях профиля высоты, названиях аэропортов, переездах между городами и
ссылках «проверить перед выездом». Экран их не показал — я их не увидела.

`i18n-check-trip.js` берёт КАЖДУЮ русскую строку в `trip-*.js` и отвечает на два
вопроса:
1. **видит ли её механизм вообще** — то есть попадает ли она в выгрузку
   `i18n-extract.js`. Не попадает — это дыра в механизме, и никакой перевод её
   не догонит;
2. **написан ли для неё английский** в `trip-<маршрут>-en.js`.

Первый же прогон нашёл **шесть полей, которых механизм не видел вовсе**:
`OPTS` (варианты дня), `ALTNM` (подписи профиля высоты), `PRECHECK` (ссылки),
`TRIP_NAME`, `AIRPORTNM` (города аэропортов), `TRANSFER` (часы переездов).
Плюс `META.dur` — его нашла таблица на Милане.

⚠️ **Правило: перед словами «маршрут переведён» — обе проверки в ноль.**
`node i18n-check.js` (движок) и `node i18n-check-trip.js` (маршруты).

---

## 4. ОНА ГОВОРИТ: «ПЕРЕВОДИМ НА НОВЫЙ ЯЗЫК»

**Это ОДИН ФАЙЛ. Ни `index.html`, ни `i18n.js` не трогаются** — правило 22б.
Так стало 08.08.2026, после шага 2 плана.

1. скопировать `lang-en.js` в `lang-<код>.js` (например `lang-es.js`);
2. перевести значения и поменять код языка в самом низу файла;
3. `git add lang-<код>.js` — ⚠️ **без этого файла не будет на сайте**: выкладка
   берёт файлы через `git ls-files` (та же ловушка, что с текстами маршрутов);
4. `node build-langs.js` — соберёт папку `/xx/`, впишет язык в карту сайта, в
   метки версий и в переключатель языков на странице;
5. `node i18n-table.js --lang xx` — та же таблица для нового языка.

Внутри файла языка лежит всё, что делает язык языком:

| раздел | что это |
|---|---|
| `DICT` | целые фразы — их заменяет `applyDomT` на готовой странице |
| `CODE` | куски, склеенные с числом или именем — их зовёт `cx()` |
| `PLURAL` | слова **и правило выбора формы**: в английском форм две, в русском три, в польском три, но выбираются иначе |
| `LOCALE` | календарь: месяцы (три формы), дни недели, **первый день недели**, знак дробной части, метка написания чисел и **образцы дат** |

Образец даты — это строка с метками, поэтому порядок слов у каждого языка свой:
`ru: '{d} {mon}'` → «20 сентября», `en: '{mon} {d}'` → «September 20»,
`es: '{d} de {mon}'` → «20 de septiembre». Движок про это не знает ничего.

⚠️ **Чего язык НЕ получает даром — скажи ей честно:** разбор фразы в анкете
(«хочу в Колорадо на 10 дней в августе») понимает только русский. Иностранцу он
не наврёт — он просто не разберёт и задаст обычные вопросы. Это не дырка
перевода, а незаделанная возможность.

**Проверено на деле 08.08.2026:** временно завели `lang-es.js` — страница
собралась по-испански целиком (даты «dom, 20 de septiembre», календарь с
испанскими днями, «3 noches», «Ciudad 2 de 4»), в `index.html` изменился ровно
один автоматически собираемый блок в голове файла. Ни строки кода.

---

## 5. КАК МЫ ПИШЕМ — ПРАВИЛО 21

Полностью — в `ROUTE-RULES.md`, правило 21. Коротко:

**Не перевод, а текст, написанный заново.** Проверка: прочитал бы это носитель
как текст сайта своей страны — или сразу видно, что переведено?

* «4 минуты пешком» → `4 min walk`, а не `4 minutes on foot`
* «Бюджет и жильё» → `What it costs`, а не `Budget and stays`
* «точка примерная» → `rough location`, а не `approximate point`

**21а. Английский американский.** Она в Майами, цены в долларах, половина
маршрутов по США. `stroller`, `Round trip`, `Gas`, `Fall`, `apartments`,
`travelers`. Смешивать нельзя — единообразие важнее выбора.

**21б. Содержимое маршрутов не переводим** — пишем заново. Отдельная работа.

**⚠️ Прочитай получившийся текст сам, прежде чем сказать «готово».** Она не знает
английского и проверить за тобой не может. Это её прямое требование.

---

## 6. ЧТО НЕ ПЕРЕВОДИМ ВООБЩЕ

Названия мест, ресторанов, экскурсий, поисковые строки — правило 10. Они живут в
оригинале на языке страны: человек должен показать таксисту то, что видит на
экране. В разметке такие места помечены `data-noT`.

---

## 7. КАРТА ФАЙЛОВ

| файл | что в нём |
|---|---|
| `i18n.js` | **механизм**: цепочка запасных языков, `t()`, `c()`, `plural()`, `date()`, русский календарь. Переводов тут больше нет |
| `lang-en.js` | **весь английский**: `DICT`, `CODE`, `PLURAL`, `LOCALE`. Грузится ТОЛЬКО на английской странице. Новый язык — такой же файл `lang-<код>.js` |
| `constructor.js` | правила сборки маршрута и сообщения проверки. **Грузится в браузер — переводится** |
| `constructor-doc.js` | памятка конструктора и самопроверка на наших маршрутах. **Терминал, в браузер не едет — не переводится.** Текст для посетителя сюда не писать |
| `i18n-check.js` | **полнота движка: «БЕЗ ПЕРЕВОДА: 0»** |
| `i18n-check-trip.js` | **полнота маршрутов: «не покрыто: 0»** |
| `i18n-extract.js` | заготовка файла текста маршрута |
| `i18n-table.js` | таблица `perevod.html` — картина для человека |
| `i18n-scan.js --left` | остаток по коду + проверка на повторы ключей |
| `build-langs.js` | собирает копию `/en/` **и переводит всю разметку на сборке**; собирает метки языков в `<head>`, `sitemap.xml` и `robots.txt`; `--check` — не устарело ли что-нибудь из этого и вся ли разметка переведена (любая беда роняет его в ошибку) |
| `sitemap.xml`, `robots.txt` | **собираются, руками не править.** Обоих файлов не было вовсе до 07.08.2026 |
| `index.html` | `tr()`, `cx()`, `applyDomT`, `trLine`, `watchDomT` |
| `ROUTE-RULES.md` | правило 21 — как пишем; правило 10 — что не трогаем |

---

## 8. ЛОВУШКИ, НА КОТОРЫХ МЫ УЖЕ ОБОЖГЛИСЬ

Не повторять — каждая стоила часа.

* **`t()` вместо `tr()`.** Однобуквенное имя столкнулось с 77 обычными
  переменными `t` в файле и уронило кабинет. У общей функции не должно быть
  однобуквенного имени.
* **Метка языка перед `<meta charset>`** — русский превращается в «Ð¡Ñ‚Ð°Ñ€Ñ‚».
  Ставить ПОСЛЕ.
* **Переключение языка после отрисовки** — куски, собранные один раз, останутся
  русскими, и прогон соврёт. Язык ставится ДО отрисовки (`window.__lang`).
* **`document.body.textContent` включает КОД скриптов** (они лежат в body).
  Прогон нашёл «min walk» в моём же комментарии. Читать копию без `<script>`.
* **Сверка пар по порядку** — на Исландии и Юте английская страница дала на 14
  кусков меньше, список сдвинулся, и таблица показала «Океан» переведённым как
  «Прилёт, Голубая лагуна». Сверять по МЕСТУ В РАЗМЕТКЕ.
* **Комментарии в `index.html` при раскладке «наше/маршрута»** — слово «вокзал»
  из пояснения делало содержимое маршрута интерфейсом. Комментарии вырезать.
* **Карту прогонами не проверить** — Leaflet в jsdom не встаёт. Только глазами.
* **«Пуш прошёл» ≠ «сайт обновился».** Проверять сам сайт.
* **⚠️ КОД 200 ОТ СЕРВЕРА НЕ ЗНАЧИТ, ЧТО ФАЙЛ ЕСТЬ.** Поймано 07.08.2026 на
  проверке «а не утекло ли лишнее». `kolibripro.com/nps-key.txt`,
  `/deploy.sh`, `/CLAUDE.md`, `/.git/config` — все ответили **200**, и на секунду
  это выглядело как утечка ключа и настроек. На деле nginx настроен подставлять
  главную страницу на любой неизвестный адрес: `/.env`, `/wp-admin`,
  `/такого-файла-нет-12345` тоже отдают 200 и тот же `index.html`.
  **Правило: смотреть не на код ответа, а на СОДЕРЖИМОЕ.** Файлы с расширением
  `.js` и `.json` подстановке не подлежат и честно отвечают 404 — по ним видно,
  что белый список выкладки работает. Обзор папок закрыт (403).
* **Проверка страницы ждала отрисовку «положенные 2,5 секунды».** Поймано
  07.08.2026. Пока машина свободна — хватало; стоило запустить рядом что-то
  тяжёлое, и `check-page.js` начинал кричать «в шапке не даты маршрута» на
  здоровых маршрутах, каждый раз на РАЗНЫХ. Я почти списала это на свою правку
  и почти пошла чинить то, что не сломано. Мера была неверной: **проверка,
  зависящая от загрузки машины, — это не проверка** (тот же изъян, что и
  «граница по месту в файле» ниже). Заодно выяснилось, что «ждать, пока текст
  перестанет меняться» ещё хуже: страница рисует ленту по СТАРОЙ сохранённой
  поездке, на секунду замирает и только потом подставляет открытый маршрут —
  затишье приходится ровно на неправильную картинку. Теперь ждём **признака
  готовности**: движок поставил во вкладку название маршрута. Три прогона
  подряд — ноль ошибок.
* **`trip-paris-en.js` — это не маршрут, а его словарь.** `check-page.js` брал
  файлы перевода как маршруты, страница честно падала на «DAY_BASE is not
  defined», и в отчёте висели четыре ошибки, которых нет.
* **`join(undefined)` — это не «подставь пустоту», а «склей через запятую».**
  Поймано 07.08.2026 первым же прогоном шага 1. Подстановка меток в шаблон
  написана через `split('{b}').join(значение)`; пропавшее название давало не
  пустое место, а ЗАПЯТУЮ: «сдают в „,“». Значение приводим к строке явно.
* **Список проверяемых файлов, записанный руками.** Тот же изъян, что и
  «граница по месту»: молчит. Пока `i18n-check.js` помнил список сам, любой
  новый `<script src>` с русским текстом остался бы невидимым, а проверка
  печатала бы ноль. Теперь список берётся из самой страницы, исключения —
  поимённо и с причиной. **Правило: проверка не должна помнить то, что может
  спросить.**
* **⚠️ ПОБЛАЖКА РАЗВИЛКАМ В САМОЙ ПРОВЕРКЕ.** Поймано 08.08.2026 на шаге 2.
  `i18n-check.js` прощал русскую строку, если рядом в той же строке кода стояло
  `LANG!=='ru'`: мол, английский вариант написан тут же. Английский — да, а
  третий язык получал его же. За этой поблажкой тихо стояла 31 фраза, и
  проверка при этом честно печатала ноль. **Правило: поблажка «рядом написано
  по-английски» — это не проверка, а её отсутствие.** Убрано; развилки теперь
  печатаются поимённо и требуют причины.
* **Разделили словари — оборвалась цепочка запасного языка.** Поймано 08.08.2026
  прогоном пробного испанского. Пока всё лежало в `i18n.js`, английский приезжал
  сам собой и закрывал дырки нового языка. После разделения на испанской
  странице грузился только испанский — и в дырке словаря снова показывалась
  КИРИЛЛИЦА, ровно то, от чего уходил шаг 0. Лечится строкой в загрузчике: язык
  не английский — везём английский вместе с ним. Экраном это не видно: страница
  выглядит нормально ровно до первой непереведённой фразы.
* **Метка `{d1}` с цифрой не подставлялась.** Подстановка в образцах дат была
  написана как `\{[a-zA-Z]+\}` — и «18–23 сентября» выходило как
  «{d1}–{d2} сентября». Мелочь, но показательная: образец пишет человек, а
  разбирает регулярка, и они разошлись молча.
* **Правка файла Питоном съедает перевод строк.** `index.html` лежит в CRLF, а
  чтение-запись текстом в Python отдаёт LF — файл целиком меняет вид, и в
  истории вместо трёх правок видно десять тысяч. Правим либо построчными
  правками, либо возвращаем CRLF после.
* **Граница перевода по МЕСТУ в файле** (было: «только до первого `<script>`»).
  Опасна тем, что молчит: допишешь разметку ниже — сборка её не увидит, страница
  мигнёт русским, и никто не узнает. Её слова 07.08.2026: «я же не программист,
  мне не понятно, когда и куда ты что допишешь, — надо сделать сразу так, чтоб
  ничего не сломать». Исправлено: граница теперь по СУТИ — пропускаем `<script>`,
  `<style>` и комментарии, всю остальную разметку переводим, где бы она ни стояла.
  **Правило: если проверка зависит от того, где именно лежит код, — это не
  проверка.**

---

## 8а. ⭐ ПОРЯДОК РАБОТ — УТВЕРЖДЁН КЛИЕНТОМ 07.08.2026

⚠️ **ЕСЛИ ТЫ НОВЫЙ ЧАТ И РЕЧЬ О ПЕРЕВОДЕ — НАЧИНАЙ ОТСЮДА.** Это не наброски.
План согласован целиком, её слова: «всё что ты предложил, все шаги мы их берём,
делаем по очереди». Ничего заново не обсуждаем — берём следующий невыполненный
шаг и делаем.

### Два правила, на которых стоит весь план

**① Языки берём ПО ОДНОМУ, английский первым и до конца.** Не «сразу все».
Английский — это испытание механизма: прогон 07.08 нашёл шесть полей, которых
механизм не видел вовсе. Каждая такая дыра находится ОДИН раз. Вели бы три языка
— заделывали бы каждую трижды. Плюс английский это девять десятых иностранных
посетителей; второй язык добавляет проценты, а стоит столько же работы.

**② Английский пишем ТОЛЬКО для доделанного маршрута.** Её слова 07.08.2026:
готовы Милан и Колорадо, остальные надо дорабатывать. Переписали маршрут — часть
английского отвалилась, `i18n-check-trip.js` это покажет, работа делается второй
раз. Порядок: доделала маршрут → сразу его английский → проверка в ноль →
следующий. **Не пачкой в конце.**

### Девять шагов. Делать строго по порядку

Против каждой строки — чем проверить, что шаг закрыт. Без проверки шаг не
закрыт, даже если «вроде сделано» (правило 3 из `CLAUDE.md`).

---

**ШАГ 0 — фундамент под ЛЮБОЙ следующий язык. ✅ СДЕЛАН 07.08.2026.**
Мелкий по работе, но без него второй язык вышел бы ХУЖЕ английского.

| что | как было | как стало |
|---|---|---|
| запасной язык | нет перевода → отдаём **русский**. Испанец видел бы кириллицу на испанской странице | цепочка **язык → английский → русский**: `chain()` и `look()` в `i18n.js`, работают и у `t()`, и у `c()`, и у `plural()` |
| метки языковых версий | `hreflang` рисовал КОД (`langLinks`) — ИИ-поисковики скриптов не выполняют, для них английской версии не существовало | лежат **текстом** в блоке `<!--LANGLINKS-->` в голове файла |
| указатель главной версии | `canonical` не было вовсе, `?trip=…&s=…` выглядели двойниками | `canonical` в том же блоке, у каждой версии свой |
| карта сайта и робот-файл | нет обоих | `sitemap.xml` (со ссылками на языковые версии) и `robots.txt` со строкой `Sitemap:` |
| честный переключатель | 10 языков, из них 8 с подписью «скоро» | только рабочие: `LANGS.filter(x=>x.ok)` |

⚠️ **Блок меток, карта сайта и робот-файл СОБИРАЮТСЯ `node build-langs.js`** из
списка готовых языков — и в копиях, и в самом `index.html`. Руками не править:
новый язык = новый словарь, код не трогаем (правило 22б). `--check` сторожит их
так же, как копию страницы: он теперь падает и на «метки устарели».

⚠️ **`sitemap.xml` и `robots.txt` надо `git add`** — выкладка берёт файлы через
`git ls-files` (та же ловушка, что с файлами текста маршрутов).

**Проверка шага (пройдена):** `node build-langs.js --check` — «свежие метки
языков, свежий sitemap.xml, свежий robots.txt, свежая /en/»; в `en/index.html`
`canonical` = `/en/` и три метки `hreflang` как текст файла; временный испанский
из семи строк отдаёт английский на непереведённых фразах, а не русский.

---

**ШАГ 1 — генератор по-английски: 248 строк в `constructor.js`. ✅ СДЕЛАН 07.08.2026.**

⚠️ **СНАЧАЛА ПОПРАВКА К САМОМУ ПЛАНУ — Я ЗДЕСЬ ОШИБАЛАСЬ, КОГДА ЕГО ПИСАЛА.**
В плане стояло «constructor.js (генератор, анкета)» и «пока анкета русская,
иностранец не может собрать поездку». **Анкета лежит не там.** Она в
`index.html` (вопросы `q:` вида «куда едете?», «водите ли вы?»), и она уже
переведена — по `index.html` проверка показывает ноль с 07.08. Иностранец
собрать поездку МОГ и до этого шага.

Что на самом деле лежит в `constructor.js` — правила сборки маршрута и
сообщения проверки: «разрыв: день 1 привёз в „B“, а день 2 стартует из „C“»,
«машину сдают раньше, чем берут». Сегодня их зовут только мы через
`window.__tripCheck()`, на экран они не выходят. Но выйдут: это ровно тот
текст, который увидит человек, когда мы покажем ему разбор его собственной
поездки, — и до тех пор он должен быть переводимым. Шаг сделан, только цена
вопроса была не «иностранец заблокирован», а «на месте мина».

**Что сделано.**
1. **Файл разделён надвое.** Браузер качал 33 КБ, а звал из них одну функцию.
   Остальное — описание деталей, тексты правил словами, ДВА НАШИХ МАРШРУТА
   ЦЕЛИКОМ и печать отчёта — памятка для нас, посетитель её не видит никогда.
   Она уехала в **`constructor-doc.js`**, который в браузер не грузится и на
   сайт не выкладывается. `node constructor.js` печатает ровно то же, что
   печатал — сверено выводом посимвольно.
   Зачем: иначе перевод требовал английского для «Узкоколейка, Меса-Верде,
   источники Пагосы» и заголовка «ЦЕПОЧКА» в терминале. Граница теперь по
   СУТИ — что грузит браузер, то и переводим (тот же приём, что с текстами
   маршрутов, правило 22г).
2. **Сообщения переписаны шаблонами с метками `{…}`, а не склейкой плюсом.**
   Было `'разрыв: день ' + a + ' привёз в «' + x + '»…'` — до такого словарь не
   достаёт никогда, и порядок слов не поменять. Стало: один ключ на всё
   сообщение, метки внутри переставляются как надо английскому. 40 ключей в
   разделе `CODE`.
3. **Перевод берётся у `i18n.js` напрямую**, а не развилками по языку.
   `index.html` не тронут ни строкой — правило 22б соблюдено.

**Проверка шага (пройдена):** `node i18n-check.js` — «в constructor.js: 0»;
`node constructor.js` — вывод совпал до знака с прежним; прогон настоящей
страницы `window.__tripCheck()` на Колорадо даёт одни и те же 11
предупреждений на `/` по-русски и на `/en/` по-английски.

⚠️ **Заодно закрыта дыра в самой проверке.** Список проверяемых файлов был
записан руками — значит подключи страница завтра ещё один файл с русским
текстом, и `i18n-check.js` честно напечатал бы ноль. Теперь он читает теги
`<script src="/…">` из `index.html` и требует, чтобы каждый подключённый файл
был либо проверен, либо назван в исключениях ПОИМЁННО и с причиной; незнакомый
файл роняет проверку в ошибку. Проверено подложенным файлом. Сейчас исключений
четыре: `i18n.js` (сам словарь), `airports.js` (названия в оригинале),
`day-math.js` и `open-hours.js` (общие с терминалом, подписи для человека
делает `index.html`).

---

**ШАГ 2 — четыре подпорки для третьего языка. ✅ СДЕЛАН 08.08.2026.**
Делался ДО второго языка нарочно: иначе то же самое пришлось бы ковырять на
каждом новом.

**① Календарь уехал к языку.** Из `index.html` пропали `MONTHS`, `WD`,
`MONTHS_EN`, `WD_EN`, `MON_EN`, `MONNM` — все шесть массивов. У языка теперь
своя запись `LOCALE`: месяцы в трёх формах, дни недели, **первый день недели**
(у нас понедельник, в Америке воскресенье — календарь в анкете рисуется как
человек привык), знак дробной части, метка написания чисел и **образцы дат**
строкой с метками. Порядок слов задаёт образец, а не код.

**② Множественное число — по языкам.** Была одна таблица `PLURAL_EN`, и любой
не-русский язык получал английские слова. Теперь у языка свои слова И СВОЁ
ПРАВИЛО выбора формы: в английском форм две, в русском три, в польском тоже три,
но выбираются иначе.

**③ Развилок `LANG!=='ru'` с текстом не осталось.** Их было 31, шестнадцать — с
английским текстом прямо в коде («City {i} of {n}», «Check in first — …», «All
the restaurants we picked — …»). Все стали шаблонами `cx('фраза {метка}',{…})`:
одна фраза одним ключом, слова внутри переставляются как надо языку. Ещё
одиннадцать развилок оказались мусором — переменная `en` уже не использовалась
или дублировала то, что и так умеет функция. Две оставшиеся текста не выбирают
и **названы поимённо с причиной в самой проверке**.

**④ Словарь языка — отдельным файлом.** `i18n.js` был 100 КБ и грузился ВСЕМ,
включая русского посетителя, которому английские фразы не нужны вовсе. Теперь в
`i18n.js` только механизм (20 КБ), а весь английский — в `lang-en.js` (100 КБ),
и он грузится только на английской странице. Русский экономит 80 КБ на каждом
заходе; на десяти языках эта экономия была бы десятикратной. Приём тот же, что у
текста маршрутов — правило 22г.

**⑤ Заодно: готовые языки стали данными.** Готовность языка стояла флажком
`ok:1` внутри `index.html` — то есть новый язык всё равно требовал правки
движка. Теперь список собирает `node build-langs.js` из самих словарей и кладёт
строкой в голову страницы. Там же поправлены два тихих места: `deploy.sh` вёз на
сервер только папку `en/` (испанский просто не уехал бы, молча), и `.gitignore`
пускал в историю тоже только `en/` и перечисленные файлы.

**Проверка шага (пройдена):** временно заведён `lang-es.js` — страница собралась
по-испански целиком: «dom, 20 de septiembre», «20–23 de septiembre de 2026»,
календарь с испанскими днями недели, «3 noches», «Ciudad 2 de 4», «4,5 h»,
«10,8 km». В `index.html` изменился ровно один автоматически собираемый блок в
голове файла — **ни строки кода**. Непереведённые фразы показали английский, а
не кириллицу (цепочка шага 0 цела). Файл после проверки удалён.
Остальные меры: `node i18n-check.js` — «БЕЗ ПЕРЕВОДА: 0» и «в constructor.js: 0»;
`node i18n-check-trip.js` — «механизм не видит: 0»; `node build-langs.js --check`
— всё свежее, «осталось русским 0»; `node check-page.js` — все восемь маршрутов
чистые; `node i18n-table.js` — интерфейс 750 из 750.

---

**ШАГ 3 — пометка «не переводить» + ворота выкладки.**
Названия, адреса, поисковые строки — правило 10, в разметке `data-noT`. Добить до
конца и добавить пометку для браузерного переводчика (`translate="no"`).
**(находка совета ⑧)** В ворота добавить проверку «файл на диске, но не в списке
выкладки»: выкладка берёт файлы через `git ls-files`, забытый `git add` даёт 404
на живом сайте — уже обжигались.
**Проверка шага:** прогон ловит подложенный файл без `git add` и падает в ошибку.

---

**ШАГ 4 — английский у доделанных маршрутов, по одному.** См. правило ② выше.
Готовы и переведены: Колорадо, Милан. Переведены, но ждут доработки маршрута:
Юта, Париж. Не трогать до готовности: Исландия, Токио, США-2.
**Проверка шага:** `node i18n-check-trip.js` — «не написано: 0» у каждого.

---

**ШАГ 4а — приёмка машинного слоя.** *(находка совета ⑥ — она считает её главной)*
Для наших маршрутов проверка есть. Для слоя, который напишет модель для
клиентской поездки, — нет никакой. Модель может: перевести название ресторана
(сломается правило 10 и поиск), потерять ключи, изменить число или время — и
английская версия разъедется с испанской.
Нужен инструмент, который ДО сохранения проверяет: все ключи на месте, названия
не тронуты, исходного языка не осталось, числа совпадают. Не прошло — не
сохраняем.
⚠️ Без него перевод клиентских поездок — **единственное место во всём проекте,
где мы верим модели на слово.** Мы так не работаем нигде больше.
Отдельно: модель недоступна или ответила плохо → честно остаёмся на исходном
языке и говорим об этом, а не показываем полупустую страницу.

---

**ШАГ 5 — она проверяет `/en/` глазами.** Анкета, кабинет, карта. Прогонами не
проверяются: Leaflet в jsdom не встаёт вовсе.

---

**ШАГ 6 — меняем местами: `kolibripro.com` по-английски, русский на `/ru/`.**
Только после шага 5, иначе посетитель увидит смесь. **(находка совета ②)** Это
переезд ВСЕХ адресов — перенаправления со старых решить ДО переезда, иначе
потеряется накопленное.

---

**ШАГ 7 — второй язык.** *(поправка совета ④)* Не «когда механизм готов», а
**когда видно, откуда идут люди**. Английский покрывает и испанца, и немца —
путешественники ищут по-английски. Второй язык оправдан под конкретный источник
притока, а не под готовность механизма.

---

### Три вещи без окончательного ответа — мои решения по умолчанию

Она ушла раньше, чем ответила. Правило «решать самой» (правила согласованы —
применять и показывать результат): работаю по этим ответам, пока не скажет иначе.

1. **Сколько языков в переключателе.** Оставляю только рабочие. Показывать
   восемь нерабочих — обещание, которое мы не выполняем. Входит в шаг 0.
2. **Ограничивать ли число переводов одной поездки.** Предел не ставлю. Ставлю
   условие: перевод только по явному нажатию (не автоматом при открытии) и слой
   кешируется навсегда. Это дешевле любого предела: 15 центов один раз на язык.
3. **Кто ручается за качество второго языка.** ⚠️ Здесь у меня нет хорошего
   ответа, и это надо сказать вслух. Сейчас гарантия — что я читаю английский
   текст сама, потому что она не знает языка. На испанском эта опора исчезает у
   нас обеих. По умолчанию беру обратный перевод другой моделью и сверку смысла
   (дёшево, ловит грубое, тонкое не поймает). Живой носитель на выборке — дороже
   и надёжнее. **Это её решение, не моё.**

### Три автора текста, механизм один

Интерфейс пишем мы (словарь). Наши маршруты пишу я заново по-английски (файл
рядом с маршрутом). Поездку клиента пишет модель на лету (слой ложится рядом,
дальше бесплатно). **Формат у всех трёх ОДИН** — поэтому шаги 1–4 и есть
фундамент под клиентские поездки, отдельно его строить не придётся.

⚠️ **Платный машинный переводчик не берём.** Замер 07.08.2026 на поездке в
30 000 знаков русского текста: Google Translate ≈ 60 центов, модель Haiku ≈ 5
центов, Sonnet ≈ 15, Opus ≈ 30. Машинный переводчик и дороже, и хуже. Браузерный
(бесплатный, на стороне клиента) оставляем кнопкой «глянуть быстро» для
черновиков, с честной оговоркой про Chrome и Edge.

---

## 9. ГДЕ МЫ СЕЙЧАС (07.08.2026, замерено прогоном обеих проверок)

* **Шаг 0 плана закрыт, ВЫЛОЖЕН И ПРОВЕРЕН НА ЖИВОМ САЙТЕ 07.08.2026.** Живой
  сайт отдаёт: `/sitemap.xml` и `/robots.txt` — 200 с настоящим содержимым; на
  `/` — `canonical` = `/` и три метки `hreflang`; на `/en/` — `canonical` =
  `/en/` и те же метки; заголовок английской вкладки приезжает сразу английским
  (`KOLIBRI Pro · route`); кнопок «скоро» в переключателе не осталось.
  Подробности — в разделе 8а. Коротко: запасной язык
  стал цепочкой «язык → английский → русский»; метки версий и `canonical` лежат
  текстом в файле, а не рисуются кодом; появились `sitemap.xml` и `robots.txt`;
  в переключателе остались только рабочие языки. Всё это собирает и сторожит
  `node build-langs.js`.
* **Шаг 1 плана закрыт 07.08.2026.** `constructor.js` разделён надвое, его
  сообщения переведены шаблонами, `index.html` при этом не тронут ни строкой.
* **Шаг 2 плана закрыт 08.08.2026.** Календарь и формы слов уехали к языку,
  развилок с текстом не осталось, словарь языка — отдельный файл
  `lang-<код>.js`. **Новый язык теперь добавляется одним файлом**, проверено
  пробным испанским. Подробности — в разделе 8а.
  **Следующий невыполненный — ШАГ 3: пометка «не переводить» (`data-noT`,
  `translate="no"`) и ворота выкладки** (поймать файл, который лежит на диске,
  но забыт в `git add`).
* **Движок — `node i18n-check.js`: весь интерфейс в нуле.**
  * `index.html` (это видит человек) — **БЕЗ ПЕРЕВОДА: 0**.
  * `constructor.js` (правила сборки и сообщения проверки) — **0**.
  * `constructor-doc.js` — памятка в терминале, в браузер не грузится и не
    переводится нарочно.
  * Проверка сама называет, какие подключённые файлы она НЕ смотрит и почему;
    незнакомый файл роняет её в ошибку.
* **Маршруты — `node i18n-check-trip.js`:** механизм не видит **0** из 1469
  русских строк — дыр в механизме нет ни одной. Английский написан для четырёх:
  * ✅ Колорадо 440, Милан 285, Юта 176, Париж 96 — «не написано: 0»
  * ⬜ Исландия 141, Токио 126, США-2 138, проба форм 67 — файла нет
* ⚠️ **Английский пишем только для доделанного маршрута.** Её слова 07.08.2026:
  готовы Милан и Колорадо, остальные надо дорабатывать. Переписал маршрут —
  проверка сама покажет отвалившиеся строки, и английский придётся дописывать
  заново. Париж и Юта уже переведены, но после доработки их надо прогнать ещё раз.
* Языков: один (`en`). Сайт по умолчанию русский, английский на `/en/`.
  Второй язык — это один файл `lang-<код>.js` и `git add`, движок не трогается.
* **Что грузит посетитель:** русский — `i18n.js` (20 КБ, механизм); англичанин —
  плюс `lang-en.js` (100 КБ); третий язык — свой файл и английский запасным.
* Её решение на будущее: kolibripro.com должен открываться по-английски, а
  русский уехать на `/ru/` — но **только когда интерфейс переведён целиком**,
  иначе посетитель увидит смесь. Порядок: доперевести → она проверяет `/en/` →
  меняем местами (это полчаса).
* **Не проверено прогонами:** анкета, кабинет и карта — их надо пройти глазами.
